# ppindices 0.1.0

Initial release: 43 plasticity indices.

## Known limitations

- `calculate_reaction_norm_non_linear`, `calculate_CVm`, and `calculate_PPF`
  have pre-existing bugs inherited from the reference implementations. Their
  documentation examples are marked `\dontrun` accordingly and should not be
  relied on until fixed in a future release.
