% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance-based.R
\name{cross_env_cov}
\alias{cross_env_cov}
\title{Cross-environment covariance and correlation}
\usage{
cross_env_cov(trait_values, env_values = NULL, return_correlation = FALSE)
}
\arguments{
\item{trait_values}{Numeric vector of trait measurements across environments.}

\item{env_values}{Optional numeric vector of environment values, the same
length as \code{trait_values}. Defaults to equidistant indices \verb{1, 2, ..., n}.}

\item{return_correlation}{Logical; if \code{TRUE}, also compute and return the
Pearson correlation between \code{trait_values} and \code{env_values}.}
}
\value{
A list with element \code{covariance} (and \code{correlation} when
\code{return_correlation = TRUE}).
}
\description{
Computes the covariance (and optionally the correlation) between trait values
and environmental values.
}
\examples{
cross_env_cov(c(2, 4, 6, 8), env_values = c(1, 2, 3, 4))
cross_env_cov(c(2, 4, 6, 8), env_values = c(1, 2, 3, 4), return_correlation = TRUE)
}
