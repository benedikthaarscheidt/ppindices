% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataframe-wrappers.R
\name{calculate_SPI}
\alias{calculate_SPI}
\title{Standardized plasticity index (SPI) for a data frame of traits}
\usage{
calculate_SPI(data, env_col, trait_cols, env1, env2, reference_env)
}
\arguments{
\item{data}{A data frame containing trait and environment columns.}

\item{env_col}{Column name (or numeric index / vector) identifying the
environment for each row of \code{data}.}

\item{trait_cols}{Character or numeric vector identifying trait columns in
\code{data}.}

\item{env1}{Label (in \code{env_col}) of the first environment to compare.}

\item{env2}{Label (in \code{env_col}) of the second environment to compare.}

\item{reference_env}{Label (in \code{env_col}) of the environment used to
estimate the standardizing standard deviation.}
}
\value{
A named numeric vector of SPI values, one per trait in
\code{trait_cols}.
}
\description{
For each trait column, computes the difference in mean trait value between
two environments, standardized by the standard deviation of a reference
environment.
}
\examples{
df <- data.frame(trait1 = c(2, 3, 4, 5, 6, 7, 8, 9), env = rep(c("E1", "E2"), each = 4))
calculate_SPI(df, env_col = "env", trait_cols = "trait1", env1 = "E1", env2 = "E2",
              reference_env = "E1")
}
