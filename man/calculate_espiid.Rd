% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distance-based.R
\name{calculate_espiid}
\alias{calculate_espiid}
\title{Environmental Sensitivity Performance Index for Individual Differences (ESPIID)}
\usage{
calculate_espiid(trait_values, use_median = FALSE)
}
\arguments{
\item{trait_values}{Numeric vector of trait measurements across equidistant
environments (each element is one observation, in environment order).}

\item{use_median}{Logical; if \code{TRUE}, use the median absolute pairwise
difference instead of the mean. Defaults to \code{FALSE}.}
}
\value{
A single numeric value: the mean ESPIID across all environment
pairs.
}
\description{
For every pair of (equidistant) environments, computes the mean (or median)
absolute pairwise difference between individual trait values in the two
environments, standardized by the distance between environment indices, and
averages across all pairs.
}
\examples{
calculate_espiid(c(2, 4, 6, 8))
}
