% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slope-based.R
\name{calculate_RC}
\alias{calculate_RC}
\title{Response coefficient (RC)}
\usage{
calculate_RC(trait_values, lower_fraction = 0.5, upper_fraction = 0.5)
}
\arguments{
\item{trait_values}{Numeric vector of trait measurements.}

\item{lower_fraction}{Fraction of sorted values (from the bottom) defining the
"low" segment. Defaults to \code{0.5}.}

\item{upper_fraction}{Fraction of sorted values (from the top) defining the
"high" segment. Defaults to \code{0.5}.}
}
\value{
A single numeric value: \code{mean(upper_values) / mean(lower_values)}.
}
\description{
Sorts trait values, splits them into low and high segments, and computes the
ratio of the high-segment mean to the low-segment mean.
}
\examples{
calculate_RC(c(2, 4, 6, 8))
}
