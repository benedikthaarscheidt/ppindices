% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index-based.R
\name{calculate_Phenotypic_Plasticity_Index}
\alias{calculate_Phenotypic_Plasticity_Index}
\title{Phenotypic plasticity index (Pi)}
\usage{
calculate_Phenotypic_Plasticity_Index(data, trait_cols = NULL)
}
\arguments{
\item{data}{A numeric vector of trait values, or a data frame / matrix
containing one or more trait columns.}

\item{trait_cols}{Optional character or numeric vector identifying trait
columns in \code{data}. When \code{NULL}, \code{data} is treated as a single numeric
vector.}
}
\value{
A single numeric value when \code{trait_cols} is \code{NULL}, or a named
numeric vector (one value per trait) otherwise.
}
\description{
Computes the classic \code{(max - min) / max} phenotypic plasticity index, either
for a single numeric vector or for each of several trait columns in a data
frame or matrix.
}
\examples{
calculate_Phenotypic_Plasticity_Index(c(2, 4, 6, 8))
df <- data.frame(trait1 = c(2, 4, 6, 8), trait2 = c(1, 3, 5, 7))
calculate_Phenotypic_Plasticity_Index(df, trait_cols = c("trait1", "trait2"))
}
