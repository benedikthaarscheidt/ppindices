% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index-based.R
\name{calculate_PFI}
\alias{calculate_PFI}
\title{Phenotypic Flexibility Index (PFI)}
\usage{
calculate_PFI(trait_values, baseline_values = NULL)
}
\arguments{
\item{trait_values}{Numeric vector of trait measurements.}

\item{baseline_values}{Optional numeric vector of baseline values, the same
length as \code{trait_values}. Defaults to the overall mean of \code{trait_values}
repeated for every sample.}
}
\value{
A single numeric value: \code{max(abs(trait - baseline)) / baseline} at
the sample of maximum deviation. Returns \code{NA} with a warning when that
baseline value is zero.
}
\description{
Identifies the sample with the largest absolute deviation from a baseline
and expresses that deviation relative to its own baseline.
}
\examples{
calculate_PFI(c(2, 4, 6, 8))
}
