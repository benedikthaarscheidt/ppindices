% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index-based.R
\name{calculate_grand_plasticity}
\alias{calculate_grand_plasticity}
\title{Grand plasticity index from adjusted environment means}
\usage{
calculate_grand_plasticity(
  trait_values,
  env_data,
  covariate_data = NULL,
  control_env = NULL
)
}
\arguments{
\item{trait_values}{Numeric vector of trait measurements.}

\item{env_data}{Vector of environment labels, the same length as
\code{trait_values}; coerced to a factor.}

\item{covariate_data}{Optional numeric covariate, the same length as
\code{trait_values}, added to the model as an additive term.}

\item{control_env}{Optional environment label to exclude from the treatment
means before computing the index.}
}
\value{
A single numeric value: \code{sd(treatment_means) / mean(treatment_means)}.
}
\description{
Fits a linear model of trait values on environment (optionally adjusting for
a covariate), extracts adjusted (least-squares) means per environment via
\code{emmeans}, and computes the coefficient of variation of those means, with an
optional exclusion of a designated control environment.
}
\examples{
trait_values <- c(2, 2.5, 3, 4, 4.5, 5, 6, 6.5, 7)
env_data <- factor(rep(c("E1", "E2", "E3"), each = 3))
calculate_grand_plasticity(trait_values, env_data)
}
