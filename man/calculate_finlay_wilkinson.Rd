% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slope-based.R
\name{calculate_finlay_wilkinson}
\alias{calculate_finlay_wilkinson}
\title{Finlay-Wilkinson stability regression}
\usage{
calculate_finlay_wilkinson(
  Y,
  genotype_ids = NULL,
  env_values = NULL,
  plot = FALSE
)
}
\arguments{
\item{Y}{A genotype-by-environment numeric matrix of trait values (rows =
genotypes, columns = environments).}

\item{genotype_ids}{Optional character or factor vector of genotype names, the
same length as \code{nrow(Y)}. Defaults to the row names of \code{Y}, or
\verb{"G1", "G2", ...} if \code{Y} has none.}

\item{env_values}{Optional numeric vector of environmental covariate values,
the same length as \code{ncol(Y)}. Defaults to the centered column means of \code{Y}.}

\item{plot}{Logical; if \code{TRUE}, plot the per-genotype regression lines.
Defaults to \code{FALSE}.}
}
\value{
A data frame with one row per genotype and columns \code{genotype}, \code{beta}
(stability slope), \code{intercept}, \code{r2}, \code{rmse}, \code{n_env}, \code{G} (genotype effect),
and \code{M} (grand mean).
}
\description{
Regresses each genotype's trait values on an environmental index (by default
the centered environment means) to estimate Finlay-Wilkinson stability
(slope), intercept, fit statistics, and genotype effects. Optionally plots
the resulting regression lines.
}
\examples{
Y <- matrix(c(2, 4, 7, 3, 6, 8, 1, 3, 4, 5, 9, 10), nrow = 4, byrow = TRUE)
calculate_finlay_wilkinson(Y)
}
