% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance-based.R
\name{calculate_CVmd}
\alias{calculate_CVmd}
\title{Coefficient of variation of medians (CVmd)}
\usage{
calculate_CVmd(trait_values, group_labels = NULL)
}
\arguments{
\item{trait_values}{Either a list of numeric vectors (one per group) or a
numeric vector of trait measurements to be split by \code{group_labels}.}

\item{group_labels}{Optional grouping vector used to split \code{trait_values} when
\code{trait_values} is not already a list.}
}
\value{
A single numeric value: the coefficient of variation of the group medians.
}
\description{
Computes the coefficient of variation across group (e.g. environment) medians
of a trait, where groups are supplied either as a list of vectors or via
\code{group_labels}.
}
\examples{
calculate_CVmd(c(2, 4, 6, 8, 10, 12), group_labels = c(1, 1, 2, 2, 3, 3))
}
