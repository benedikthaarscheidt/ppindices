% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance-based.R
\name{calculate_CVm}
\alias{calculate_CVm}
\title{Coefficient of variation of means (CVm)}
\usage{
calculate_CVm(trait_values, env_values = NULL)
}
\arguments{
\item{trait_values}{Either a list of numeric vectors (one per group) or a
numeric vector of trait measurements to be split by \code{env_values}.}

\item{env_values}{Optional grouping vector used to split \code{trait_values} when
\code{trait_values} is not already a list.}
}
\value{
A single numeric value: the coefficient of variation of the group means.
}
\description{
Computes the coefficient of variation across group (e.g. environment) means of
a trait, where groups are supplied either as a list of vectors or via
\code{group_labels}.
}
\examples{
calculate_CVm(list(c(2, 3, 4), c(5, 6, 7), c(1, 2, 3)))
calculate_CVm(c(2, 3, 4, 6, 7, 8), env_values = c(1, 1, 1, 2, 2, 2))
}
