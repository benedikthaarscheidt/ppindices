% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index-based.R
\name{calculate_MVPi}
\alias{calculate_MVPi}
\title{Multivariate plasticity index (MVPi)}
\usage{
calculate_MVPi(trait_data, env = NULL, n_axes = 1)
}
\arguments{
\item{trait_data}{A numeric matrix (or object coercible to one) of trait
values, with samples in rows and traits in columns.}

\item{env}{Optional vector of environment labels, the same length as
\code{nrow(trait_data)}. Defaults to equidistant indices \verb{1, 2, ..., n}.}

\item{n_axes}{Number of leading principal component axes to retain. Defaults
to \code{1}.}
}
\value{
A single numeric value: the mean pairwise Euclidean distance between
environment centroids in PCA space. Returns \code{NA} with a warning when fewer
than two environments are present.
}
\description{
Runs a PCA on a matrix of trait values, retains the first \code{n_axes} principal
components, computes per-environment centroids in that reduced space, and
summarizes plasticity as the mean pairwise Euclidean distance between
centroids.
}
\examples{
trait_data <- matrix(c(1, 2, 3, 2, 3, 4, 5, 6, 5, 6, 7, 6), ncol = 3, byrow = TRUE)
env <- c("E1", "E1", "E2", "E2")
calculate_MVPi(trait_data, env = env, n_axes = 2)
}
