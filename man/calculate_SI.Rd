% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance-based.R
\name{calculate_SI}
\alias{calculate_SI}
\title{Stability index (SI)}
\usage{
calculate_SI(trait_values, env = NULL)
}
\arguments{
\item{trait_values}{Numeric vector of trait measurements across environments.}

\item{env}{Optional grouping vector of environment labels, the same length as
\code{trait_values}. Defaults to equidistant indices \verb{1, 2, ..., n} (each
observation its own environment).}
}
\value{
A single numeric value: \code{var(env_means) / mean(env_means)}. Returns
\code{NA} with a warning when the overall mean of environment means is zero.
}
\description{
Computes a stability index as the variance of environment-level trait means
relative to their overall mean.
}
\examples{
calculate_SI(c(2, 4, 6, 8), env = c(1, 1, 2, 2))
}
