% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distance-based.R
\name{calculate_ESPI}
\alias{calculate_ESPI}
\title{Environment-standardized plasticity index (ESPI)}
\usage{
calculate_ESPI(trait_values, env_values = NULL)
}
\arguments{
\item{trait_values}{A numeric vector of trait measurements, or a data frame
where each column is a trait.}

\item{env_values}{Optional vector of environment values. For the
single-trait (vector) form it must have the same length as \code{trait_values}.
Defaults to equidistant indices \verb{1, 2, ..., n}.}
}
\value{
A single numeric value for a vector input, or a named numeric vector
(one value per trait column) for a data frame input.
}
\description{
Computes the range of environment-level trait means, standardized by the
range of the environment values. Accepts a single trait (numeric vector) or
multiple traits (columns of a data frame).
}
\examples{
calculate_ESPI(c(2, 3, 6, 7), env_values = c(1, 1, 2, 2))
}
