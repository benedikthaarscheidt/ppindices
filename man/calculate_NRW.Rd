% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index-based.R
\name{calculate_NRW}
\alias{calculate_NRW}
\title{Norm-of-reaction width (NRW)}
\usage{
calculate_NRW(
  trait_values,
  env_values = NULL,
  group_values = NULL,
  across = FALSE
)
}
\arguments{
\item{trait_values}{Numeric vector of trait measurements.}

\item{env_values}{Optional vector of environment labels, the same length as
\code{trait_values}. Defaults to equidistant indices \verb{1, 2, ..., n}.}

\item{group_values}{Optional vector of group labels (e.g. genotype), the
same length as \code{trait_values}, to compute NRW separately per group.}

\item{across}{Logical; if \code{TRUE}, compute the range directly across raw
\code{trait_values} (ignoring environment grouping). Defaults to \code{FALSE}.}
}
\value{
A single numeric value (overall or per environment-mean range), or a
named numeric vector of per-group values when \code{group_values} is supplied.
}
\description{
Computes the range (max - min) of trait values, at the level of raw
observations, environment means, or per-group environment means.
}
\examples{
calculate_NRW(c(2, 4, 6, 8), env_values = c(1, 1, 2, 2))
}
