% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index-based.R
\name{calculate_PPF}
\alias{calculate_PPF}
\title{Phenotypic Plasticity Factor (PPF)}
\usage{
calculate_PPF(
  trait_values,
  env_groups,
  covariate_values = NULL,
  env_pairs = NULL
)
}
\arguments{
\item{trait_values}{Numeric vector of trait measurements.}

\item{env_groups}{Vector of environment group labels, the same length as
\code{trait_values}; coerced to a factor.}

\item{covariate_values}{Optional numeric vector or data frame of covariates
to include additively in the model.}

\item{env_pairs}{Optional list of environment-label pairs to evaluate.
Defaults to all pairwise combinations of \code{levels(env_groups)}.}
}
\value{
A single numeric value: the mean, across environment pairs, of
\code{100 * abs(LSM1 - LSM2) / LSM1}.
}
\description{
For each pair of environment groups (all pairs by default), fits a linear
model and computes the percentage difference between the least-squares means
of the pair, then averages across pairs.
}
\note{
The example below is illustrative only and is wrapped in \verb{\dontrun{}}.
The function fits each pairwise model on locally renamed variables
(\code{subset_trait_values}, \code{subset_env_groups}) but then calls
\code{emmeans::emmeans(model, ~ env_groups)}, which looks for a term named
\code{env_groups} that the model does not contain, so it errors with
"No variable named env_groups in the reference grid".
}
\examples{
\dontrun{
trait_values <- c(2, 2.5, 3, 4, 4.5, 5)
env_groups <- factor(rep(c("A", "B"), each = 3))
calculate_PPF(trait_values, env_groups)
}
}
