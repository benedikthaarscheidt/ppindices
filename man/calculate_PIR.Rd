% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slope-based.R
\name{calculate_PIR}
\alias{calculate_PIR}
\title{Plasticity index at relative growth rate maximum (PIR)}
\usage{
calculate_PIR(trait_values, env_values = NULL, rgr_values = NULL)
}
\arguments{
\item{trait_values}{Numeric vector of trait measurements across environments.}

\item{env_values}{Optional grouping vector of environment labels, the same
length as \code{trait_values}. Defaults to equidistant indices \verb{1, 2, ..., n}
(each observation its own environment).}

\item{rgr_values}{Optional numeric vector of pre-computed relative growth
rates, one per observation, averaged per environment. When \code{NULL}, relative
growth rates are derived from successive environment means.}
}
\value{
A single numeric value: \code{(max_mean - min_mean) / mean_at_max_rgr}.
}
\description{
Computes environment-level means of a trait, estimates (or accepts) relative
growth rates between successive environments, and expresses the trait range
relative to the mean at the environment of maximum relative growth rate.
}
\examples{
calculate_PIR(c(2, 4, 6, 8))
}
