% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distance-based.R
\name{calculate_FPI}
\alias{calculate_FPI}
\title{Fitness-relative plasticity index (FPI)}
\usage{
calculate_FPI(trait_values, env_values = NULL, control_stress = NULL)
}
\arguments{
\item{trait_values}{Numeric vector of trait measurements.}

\item{env_values}{Optional vector of environment labels, the same length as
\code{trait_values}. Defaults to equidistant indices \verb{1, 2, ..., n}.}

\item{control_stress}{Optional vector, the same length as \code{trait_values},
labelling each observation as \code{0}/\code{1} or \code{"Control"}/\code{"Stress"}. When
supplied, \code{env_values} pairing is bypassed in favor of a direct
control-vs-stress comparison.}
}
\value{
A single numeric value: the mean fractional change in trait value.
Returns \code{NA} with a warning when only one unique environment is present.
}
\description{
Computes the relative change in trait value between environments (or
between control and stress conditions), expressed as a fraction of the
baseline (control / first) value, averaged across environment pairs.
}
\examples{
calculate_FPI(c(2, 4, 6, 8), env_values = c(1, 1, 2, 2))
}
