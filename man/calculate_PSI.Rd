% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variance-based.R
\name{calculate_PSI}
\alias{calculate_PSI}
\title{Plasticity stability score (PSI)}
\usage{
calculate_PSI(trait_values, env_values = NULL)
}
\arguments{
\item{trait_values}{Numeric vector of trait measurements across environments.}

\item{env_values}{Optional numeric vector of environment values, the same
length as \code{trait_values}. Defaults to equidistant indices \verb{1, 2, ..., n}.}
}
\value{
A single numeric value in \verb{(0, 1]}: \code{1 / (1 + abs(slope))}, where
\code{slope} is the coefficient from regressing \code{trait_values} on \code{env_values}.
}
\description{
Fits a linear regression of trait values on environmental values and converts
the slope into a bounded stability score.
}
\examples{
calculate_PSI(c(2, 4, 6, 8), env_values = c(1, 2, 3, 4))
}
