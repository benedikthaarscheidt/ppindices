% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index-based.R
\name{calculate_PR}
\alias{calculate_PR}
\title{Phenotypic Range (PR)}
\usage{
calculate_PR(trait_values, env_values = NULL, across = TRUE)
}
\arguments{
\item{trait_values}{Numeric vector of trait measurements across environments.}

\item{env_values}{Optional vector of environment labels, the same length as
\code{trait_values}. Defaults to equidistant indices \verb{1, 2, ..., n}.}

\item{across}{Logical; if \code{TRUE} (default), compute a single range across all
environments. If \code{FALSE}, compute the range within each unique environment.}
}
\value{
A single numeric value when \code{across = TRUE}, or a numeric vector (one
value per unique environment) when \code{across = FALSE}.
}
\description{
Computes the range (max - min) of trait values, either across all
environments at once or separately within each unique environment.
}
\examples{
calculate_PR(c(2, 4, 6, 8), env_values = c(1, 2, 3, 4))
calculate_PR(c(2, 4, 6, 8), env_values = c(1, 1, 2, 2), across = FALSE)
}
