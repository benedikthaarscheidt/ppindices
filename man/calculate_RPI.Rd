% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index-based.R
\name{calculate_RPI}
\alias{calculate_RPI}
\title{Relative plasticity index (RPI)}
\usage{
calculate_RPI(trait_values, env_values = NULL, env1 = NULL, env2 = NULL)
}
\arguments{
\item{trait_values}{Numeric vector of trait measurements.}

\item{env_values}{Optional vector of environment labels, the same length as
\code{trait_values}. Defaults to equidistant indices \verb{1, 2, ..., n}.}

\item{env1}{Optional label of the first environment in a specific pair to
compare.}

\item{env2}{Optional label of the second environment in a specific pair to
compare.}
}
\value{
A single numeric value: the mean relative distance
\code{abs(x1 - x2) / (x1 + x2)}, for the requested pair or averaged over all
pairs. Returns \code{NA} with a warning when fewer than two unique environments
are present.
}
\description{
Computes the relative distance between trait values recorded in different
environments, either for a single specified environment pair or averaged
across all pairwise combinations of environments.
}
\examples{
calculate_RPI(c(2, 3, 6, 7), env_values = c(1, 1, 2, 2))
}
