% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataframe-wrappers.R
\name{rdpi_mean_calculation}
\alias{rdpi_mean_calculation}
\title{RDPI across environment combinations of a data frame}
\usage{
rdpi_mean_calculation(
  dataframe,
  trait_cols,
  sp = NULL,
  factors = NULL,
  factors_not_in_dataframe = NULL,
  stat_analysis = NULL
)
}
\arguments{
\item{dataframe}{A data frame containing trait, environment/factor, and
(optionally) species columns.}

\item{trait_cols}{Character or numeric vector identifying trait columns in
\code{dataframe}.}

\item{sp}{Optional column name or index identifying a species/group column
used to split the analysis.}

\item{factors}{Optional character vector of column names (or numeric column
indices) in \code{dataframe} combined into the environment factor.}

\item{factors_not_in_dataframe}{Optional list of vectors, external to
\code{dataframe}, combined with (or in place of) \code{factors}.}

\item{stat_analysis}{Optional flag; when non-\code{NULL}, also runs ANOVA and
Tukey's HSD per trait and returns boxplots.}
}
\value{
A data frame of RDPI values with columns \code{sp}, \code{env1}, \code{env2},
\code{rdpi} (one row per species x trait x environment pair). When
\code{stat_analysis} is supplied, returns a list with elements \code{rdpi_results},
\code{trait_boxplots}, \code{anova_results}, and \code{tukey_results}.
}
\description{
Builds a combined environment factor from one or more columns of a data
frame (optionally split by species/group), computes the Relative Distance
Plasticity Index (RDPI) between every pair of combined environment levels
for each trait, and optionally runs ANOVA and Tukey's HSD across the
combined factor levels.
}
\examples{
df <- data.frame(
  trait1 = c(2, 3, 4, 5, 6, 7, 8, 9),
  envA = rep(c("low", "high"), each = 4),
  envB = rep(c("x", "y"), times = 4)
)
rdpi_mean_calculation(df, trait_cols = "trait1", factors = c("envA", "envB"))
}
